Due to some matrix calculations I was doing in SLiM via a call to R, we ended up using more memory
than the HPC had, leading to crashes. These scripts test a new approach to solving that problem without
calling R from within SLiM